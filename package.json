{
  "name": "tatry-field",
  "version": "0.8.0",
  "private": true,
  "description": "Safety-first editorial hiking guide for the Polish Tatras — routes, field maps, comparison, logistics and preparation",
  "scripts": {
    "start": "node server.js",
    "build:tracks": "node scripts/build-verified-tracks.js",
    "test": "node scripts/check-tracks.js && node scripts/check-field-maps.js && node scripts/check-trip-intelligence.js && node scripts/check-group-votes.js && node scripts/check-home-base.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "leaflet": "1.9.4"
  }
}
